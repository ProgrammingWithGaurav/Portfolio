/** @type {import('next').NextConfig} */
const nextConfig = {
    // image optimization
    images: {
        domains: ['aceternity.com', 'unsplash.com', "avatars.githubusercontent.com", 'images.unsplash.com', 'pexels.com', 'images.pexels.com'],
    },
};

export default nextConfig;
